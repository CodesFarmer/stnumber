set(CMAKE_PREFIX_PATH "/home/slam/UntouchWorkspace/Thirdparty/free/x86_64-linux/CMake-hdf5-1.10.1/hdf5-1.10.1/build/install/share/cmake;${CMAKE_PREFIX_PATH}")

#set (LIB_TYPE SHARED) # or SHARED
#string(TOLOWER ${LIB_TYPE} SEARCH_TYPE)
#find_package (HDF5 NAMES hdf5 COMPONENTS C ${SEARCH_TYPE})

find_package(HDF5)

link_libraries(/home/slam/UntouchWorkspace/Thirdparty/free/x86_64-linux/CMake-hdf5-1.10.1/hdf5-1.10.1/build/install/lib)
if(HDF5_FOUND)
#  list(APPEND DEST_LINK_TARGETS ${HDF5_EXPORT_LIBRARIES})
#  list(APPEND DEST_LINK_TARGETS ${HDF5_MPI_C_LIBRARIES})
#  list(APPEND DEST_LINK_TARGETS ${INTERFACE_LINK_LIBRARIES})
  list(APPEND DEST_LINK_TARGETS ${HDF5_LINK_LIBRARIES})
  list(APPEND DEST_INCLUDE_DIRS ${HDF5_INCLUDE_DIR})
  message(STATUS "Compile With HDF5 : ${HDF5_LINK_LIBRARIES}")
else()
  message(STATUS "Can not find HDF5...")
endif()
#include_directories(${DEST_INCLUDE_DIRS})
